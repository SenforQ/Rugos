
//: Declare String Begin

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let loggerAppFlag:[UInt8] = [0x61,0x6a,0x66,0x70,0x68,0x60,0x6b,0x71,0x2b,0x62,0x60,0x71,0x40,0x69,0x60,0x68,0x60,0x6b,0x71,0x76,0x47,0x7c,0x51,0x64,0x62,0x4b,0x64,0x68,0x60,0x2d,0x22,0x67,0x6a,0x61,0x7c,0x22,0x2c,0x5e,0x35,0x58,0x2b,0x76,0x71,0x7c,0x69,0x60,0x2b,0x67,0x64,0x66,0x6e,0x62,0x77,0x6a,0x70,0x6b,0x61,0x38,0x22,0x77,0x62,0x67,0x64,0x2d,0x35,0x29,0x35,0x29,0x35,0x29,0x35,0x2c,0x22]

private func policyFilter(list num: UInt8) -> UInt8 {
    return num ^ 5
}

/*: "closeWeb" :*/
fileprivate let appCurrencyIndexData:String = "try trigger native remotecloseWeb"

/*: "toUrl" :*/
fileprivate let notiCornerAddState:[Character] = ["t","o","U","r","l"]

/*: "syncAppInfo" :*/
fileprivate let bridgeModelResult:String = "SY"
fileprivate let serviceLaunchPresentMode:String = "ncApnetwork group copy allow save"

/*: "typeName" :*/
fileprivate let user_presentationState:String = "typeNamecontrast case fire link"

/*: "isAuth" :*/
fileprivate let main_wirelessTelephoneFormat:String = "isAuthphone calendar create network let"

/*: "isFirst" :*/
fileprivate let controllerValidPath:String = "trust command time combine makeisFirst"

/*: "Please click 'Go' to allow access" :*/
fileprivate let notiClickMotiveTime:[UInt8] = [0xb7,0x8b,0x82,0x86,0x94,0x82,0xc7,0x84,0x8b,0x8e,0x84,0x8c,0xc7,0xc0,0xa0,0x88,0xc0,0xc7,0x93,0x88,0xc7,0x86,0x8b,0x8b,0x88,0x90,0xc7,0x86,0x84,0x84,0x82,0x94,0x94]

private func poorLayer(document num: UInt8) -> UInt8 {
    return num ^ 231
}

/*: "getCameraStatus" :*/
fileprivate let controllerSucceedDate:String = "map behavior selectedgetCa"
fileprivate let parserNationalServiceString:String = "itemera"
fileprivate let kPromptSecret:[Character] = ["S","t","a","t","u","s"]

/*: "Please allow ' :*/
fileprivate let noti_dateActivityVersion:[Character] = ["P","l","e","a","s","e"," ","a"]
fileprivate let app_accessName:String = "llow \'"

/*: ' to access your camera in your iPhone's 'Settings-Privacy-Camera' option" :*/
fileprivate let cacheEnterError:[UInt8] = [0xad,0xaa,0xfe,0xe5,0xaa,0xeb,0xe9,0xe9,0xef,0xf9,0xf9,0xaa,0xf3,0xe5,0xff,0xf8,0xaa,0xe9,0xeb,0xe7,0xef,0xf8,0xeb,0xaa,0xe3,0xe4,0xaa,0xf3,0xe5,0xff,0xf8,0xaa,0xe3,0xda,0xe2,0xe5,0xe4,0xef,0xad,0xf9,0xaa,0xad,0xd9,0xef,0xfe,0xfe,0xe3,0xe4,0xed,0xf9,0xa7,0xda,0xf8,0xe3,0xfc,0xeb,0xe9,0xf3,0xa7,0xc9,0xeb,0xe7,0xef,0xf8,0xeb,0xad,0xaa,0xe5,0xfa,0xfe,0xe3,0xe5,0xe4]

private func albumVersion(appear num: UInt8) -> UInt8 {
    return num ^ 138
}

/*: "getPhotoStatus" :*/
fileprivate let managerCoreMsg:String = "filter dismissgetPh"
fileprivate let constCanString:[Character] = ["a","t","u","s"]

/*: ' to access your album in your iPhone's 'Settings-Privacy-Album' option" :*/
fileprivate let userInsertList:[UInt8] = [0xac,0xab,0xff,0xe4,0xab,0xea,0xe8,0xe8,0xee,0xf8,0xf8,0xab,0xf2,0xe4,0xfe,0xf9,0xab,0xea,0xe7,0xe9,0xfe,0xe6,0xab,0xe2,0xe5,0xab,0xf2,0xe4,0xfe,0xf9,0xab,0xe2,0xdb,0xe3,0xe4,0xe5,0xee,0xac,0xf8,0xab,0xac,0xd8,0xee,0xff,0xff,0xe2,0xe5,0xec,0xf8,0xa6,0xdb,0xf9,0xe2,0xfd,0xea,0xe8,0xf2,0xa6,0xca,0xe7,0xe9,0xfe,0xe6,0xac,0xab,0xe4,0xfb,0xff,0xe2,0xe4,0xe5]

private func kickDoingeSHeels(create num: UInt8) -> UInt8 {
    return num ^ 139
}

/*: "getMicStatus" :*/
fileprivate let data_padFactoryFrameDate:String = "getMmodel inside where please large"
fileprivate let serviceTextMessage:[Character] = ["s"]

/*: ' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option" :*/
fileprivate let kWillID:[UInt8] = [0x15,0x12,0x46,0x5d,0x12,0x53,0x51,0x51,0x57,0x41,0x41,0x12,0x4b,0x5d,0x47,0x40,0x12,0x5f,0x5b,0x51,0x40,0x5d,0x42,0x5a,0x5d,0x5c,0x57,0x12,0x5b,0x5c,0x12,0x4b,0x5d,0x47,0x40,0x12,0x5b,0x62,0x5a,0x5d,0x5c,0x57,0x15,0x41,0x12,0x15,0x61,0x57,0x46,0x46,0x5b,0x5c,0x55,0x41,0x1f,0x62,0x40,0x5b,0x44,0x53,0x51,0x4b,0x1f,0x7f,0x5b,0x51,0x40,0x5d,0x42,0x5a,0x5d,0x5c,0x57,0x15,0x12,0x5d,0x42,0x46,0x5b,0x5d,0x5c]

private func styleInstance(enter num: UInt8) -> UInt8 {
    return num ^ 50
}

/*: "Cancel" :*/
fileprivate let factorySharedProgressUrl:[Character] = ["C","a","n","c","e","l"]

/*: "Poor network, loading failed" :*/
fileprivate let kModeVersion:String = "mirror normal available securePoor "
fileprivate let factoryMilitaryPostString:String = "ork,enter now index"
fileprivate let appTableList:[Character] = ["n","g"," ","f","a","i","l","e","d"]

/*: "Refresh" :*/
fileprivate let kDoingHandState:[Character] = ["R","e","f","r","e","s","h"]

/*: "Alert" :*/
fileprivate let userBiologyLabPath:String = "Alertconfirm across"

/*: "OK" :*/
fileprivate let main_componentPath:[Character] = ["O","K"]

/*: "Done" :*/
fileprivate let loggerSecondStr:String = "Doneas calendar"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let dataRawStatus:[UInt8] = [0x16,0x42,0x42,0x3e,0x22,0x3d,0x3d,0x3a,0xfc,0x1c,0x2f,0x42,0x37,0x44,0x33,0x22,0x3d,0x18,0x41,0xf6,0xf5,0x40,0x33,0x31,0x36,0x2f,0x40,0x35,0x33,0xf5,0xf7]

fileprivate func launchTool(ok num: UInt8) -> UInt8 {
    let value = Int(num) - 206
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onPageShow" :*/
fileprivate let managerLargeTitle:String = "onPagchange ting auto at"

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let dataServiceId:[UInt8] = [0x3b,0x29,0x28,0x77,0x6f,0x68,0x53,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x77,0x6f,0x68,0x53,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "onPageHide" :*/
fileprivate let cacheEndPath:String = "enter foundation activeonPa"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let userResultDict:[UInt8] = [0xe9,0xdb,0xe0,0xd6,0xe1,0xe9,0xa0,0xe1,0xe0,0xc2,0xd3,0xd9,0xd7,0xba,0xdb,0xd6,0xd7,0x98,0x98,0xe1,0xe0,0xc2,0xd3,0xd9,0xd7,0xba,0xdb,0xd6,0xd7,0x9a,0x9b,0xad]

fileprivate func goHour(total num: UInt8) -> UInt8 {
    let value = Int(num) - 114
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ViewController.swift
//  OverseaH5
//
//  Created by DouXiu on 2025/9/23.
//

//: import UIKit
import UIKit
//: import WebViewJavascriptBridge
import WebViewJavascriptBridge
//: import WebKit
import WebKit

//: class AppWebViewController: UIViewController {
class DeviceViewController: UIViewController {
    
    //: var urlString: String = ""
    var urlString: String = ""
    /// 是否背景透明
    //: var clearBgColor = false
    var clearBgColor = false
    /// 是否全屏
    //: var fullscreen = true
    var fullscreen = true
    
    //: private var bridge: WebViewJavascriptBridge?
    private var bridge: WebViewJavascriptBridge?
    
    // Pending JS dialog completion handlers (ensure always-called to avoid WKWebView crash)
    //: private var pendingAlertCompletion: (() -> Void)?
    private var pendingAlertCompletion: (() -> Void)?
    //: private var pendingConfirmCompletion: ((Bool) -> Void)?
    private var pendingConfirmCompletion: ((Bool) -> Void)?
    //: private var pendingPromptCompletion: ((String?) -> Void)?
    private var pendingPromptCompletion: ((String?) -> Void)?

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []
        //: let userControl = WKUserContentController()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: let w = WKWebView(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: w.scrollView.contentInsetAdjustmentBehavior = .never
        w.scrollView.contentInsetAdjustmentBehavior = .never
        //: w.isOpaque = false
        w.isOpaque = false
        //: w.scrollView.bounces = false
        w.scrollView.bounces = false
        //: w.scrollView.alwaysBounceVertical = false
        w.scrollView.alwaysBounceVertical = false
        //: w.scrollView.alwaysBounceHorizontal = false
        w.scrollView.alwaysBounceHorizontal = false
        //: return w
        return w
    //: }()
    }()

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.addSubview(self.webView)
        view.addSubview(self.webView)
        //: var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        //: if fullscreen == false {
        if fullscreen == false {
            //: frame.origin.y = AppConfig.getStatusBarHeight()
            frame.origin.y = PhoneAdapt.takeAPowder()
        }
        //: self.webView.frame = frame
        self.webView.frame = frame
 
        //: self.addBridgeMethod()
        self.hand()
        //: self.beginStartRequest()
        self.color()
 
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(deadline),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }
    
    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        deadline()
    }
    
    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        system()
        //: finalizePendingJSHandlersIfNeeded()
        sinceSavePolicy()
    }

    //: deinit {
    deinit {
        //: removeBridgeMethod()
        finish()
        //: finalizePendingJSHandlersIfNeeded()
        sinceSavePolicy()
    }

    /// 发起网页请求
    //: private func beginStartRequest() {
    private func color() {
        //: if let url = URL(string: urlString) {
        if let url = URL(string: urlString) {
            //: let urlRequest = URLRequest(url: url)
            let urlRequest = URLRequest(url: url)
            //: self.webView.load(urlRequest)
            self.webView.load(urlRequest)
            //: self.clearJSBgColor()
            self.status()
        }
    }
    
    /// 设置页面为透明
    //: private func clearJSBgColor() {
    private func status() {
        //: guard clearBgColor == true else { return }
        guard clearBgColor == true else { return }
        //: webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
        webView.evaluateJavaScript(String(bytes: loggerAppFlag.map{policyFilter(list: $0)}, encoding: .utf8)!) { _, _  in
        }
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: webView.backgroundColor = .clear
        webView.backgroundColor = .clear
        //: webView.scrollView.backgroundColor = .clear
        webView.scrollView.backgroundColor = .clear
        //: webView.scrollView.bounces = false
        webView.scrollView.bounces = false
        //: webView.scrollView.alwaysBounceVertical = false
        webView.scrollView.alwaysBounceVertical = false
        //: webView.scrollView.alwaysBounceHorizontal = false
        webView.scrollView.alwaysBounceHorizontal = false
        //: webView.isOpaque = false
        webView.isOpaque = false
    }
    
    /// 关闭webview事件
    //: func closeWeb() {
    func emptyBegin() {
        //: if webView.canGoBack {
        if webView.canGoBack {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        
        //: removeBridgeMethod()
        finish()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = AppConfig.currentViewController() {
                if let currentVC = PhoneAdapt.purchase() {
                    //: if currentVC.isKind(of: AppWebViewController.self) {
                    if currentVC.isKind(of: DeviceViewController.self) {
                        //: (currentVC as! AppWebViewController).jsEvent_onPageShow()
                        (currentVC as! DeviceViewController).deadline()
                    }
                }
            }
        }
    }
}

//: extension AppWebViewController: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
extension DeviceViewController: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
    //: func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
    func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
        //: return ""
        return ""
    }

    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: print("js call method name = \(message.name), message = \(message.body)")
        // 兼容老事件
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: let type = message.name
            let type = message.name
            //: if type == "closeWeb" {
            if type == (String(appCurrencyIndexData.suffix(8))) {
                //: self.closeWeb()
                self.emptyBegin()
                
            //: } else if type == "toUrl" {
            } else if type == (String(notiCornerAddState)) {
                //: if let url = message.body as? String {
                if let url = message.body as? String {
                    //: AppWebViewController.openNewWebView(url)
                    DeviceViewController.administer(url)
                }
            }
        }
    }

    //: func addBridgeMethod() {
    func hand() {
        //: self.bridge = WebViewJavascriptBridge(self.webView)
        self.bridge = WebViewJavascriptBridge(self.webView)
        //: self.bridge?.setWebViewDelegate(self)
        self.bridge?.setWebViewDelegate(self)
        //: self.bridge?.registerHandler("syncAppInfo", handler: { data, callback in
        self.bridge?.registerHandler((bridgeModelResult.lowercased() + String(serviceLaunchPresentMode.prefix(4)) + "pInfo"), handler: { data, callback in
            //: print("js call getUserIdFromObjC, data from js is %@", data as Any)
            //: if callback != nil {
            if callback != nil {
                //: if let dic = data as? [String: Any] {
                if let dic = data as? [String: Any] {
                    //: self.handleH5Message(schemeDic: dic) { backDic in
                    self.rationalMotiveHouseOfCards(schemeDic: dic) { backDic in
                        //: callback?(backDic)
                        callback?(backDic)
                        //: DispatchQueue.main.async {
                        DispatchQueue.main.async {
                            //: self.handAuthOpenURL(dic: backDic)
                            self.post(dic: backDic)
                        }
                    }
                }
            }
        //: })
        })
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "closeWeb")
        ucController.add(JoinNet(self), name: (String(appCurrencyIndexData.suffix(8))))
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "toUrl")
        ucController.add(JoinNet(self), name: (String(notiCornerAddState)))
    }

    //: func removeBridgeMethod() {
    func finish() {
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            //: ucController.removeAllScriptMessageHandlers()
            ucController.removeAllScriptMessageHandlers()
        //: } else {
        } else {
            //: ucController.removeScriptMessageHandler(forName: "closeWeb")
            ucController.removeScriptMessageHandler(forName: (String(appCurrencyIndexData.suffix(8))))
            //: ucController.removeScriptMessageHandler(forName: "toUrl")
            ucController.removeScriptMessageHandler(forName: (String(notiCornerAddState)))
        }
    }

    //: func handAuthOpenURL(dic: [String: Any]) {
    func post(dic: [String: Any]) {
        //: if let typeName = dic["typeName"] as? String, let isAuth = dic["isAuth"] as? Bool, let isFirst = dic["isFirst"] as? Bool {
        if let typeName = dic[(String(user_presentationState.prefix(8)))] as? String, let isAuth = dic[(String(main_wirelessTelephoneFormat.prefix(6)))] as? Bool, let isFirst = dic[(String(controllerValidPath.suffix(7)))] as? Bool {
            //: if isAuth || isFirst {
            if isAuth || isFirst {
                //: return
                return
            }
            //: var message = "Please click 'Go' to allow access"
            var message = String(bytes: notiClickMotiveTime.map{poorLayer(document: $0)}, encoding: .utf8)!
            //: var needAlert = false
            var needAlert = false
            //: if typeName == "getCameraStatus" {
            if typeName == (String(controllerSucceedDate.suffix(5)) + parserNationalServiceString.replacingOccurrences(of: "item", with: "m") + String(kPromptSecret)) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your camera in your iPhone's 'Settings-Privacy-Camera' option"
                message = (String(noti_dateActivityVersion) + app_accessName) + "\(cacheWarnVersionURL)" + String(bytes: cacheEnterError.map{albumVersion(appear: $0)}, encoding: .utf8)!
                
            //: } else if typeName == "getPhotoStatus" {
            } else if typeName == (String(managerCoreMsg.suffix(5)) + "otoSt" + String(constCanString)) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your album in your iPhone's 'Settings-Privacy-Album' option"
                message = (String(noti_dateActivityVersion) + app_accessName) + "\(cacheWarnVersionURL)" + String(bytes: userInsertList.map{kickDoingeSHeels(create: $0)}, encoding: .utf8)!
                
            //: } else if typeName == "getMicStatus" {
            } else if typeName == (String(data_padFactoryFrameDate.prefix(4)) + "icStatu" + String(serviceTextMessage)) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option"
                message = (String(noti_dateActivityVersion) + app_accessName) + "\(cacheWarnVersionURL)" + String(bytes: kWillID.map{styleInstance(enter: $0)}, encoding: .utf8)!
            }

            //: if needAlert {
            if needAlert {
                //: let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)
                let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)

                //: let action1 = UIAlertAction(title: "Cancel", style: .default) { _ in
                let action1 = UIAlertAction(title: (String(factorySharedProgressUrl)), style: .default) { _ in
                }
                //: let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                    //: if let url = URL(string: UIApplication.openSettingsURLString) {
                    if let url = URL(string: UIApplication.openSettingsURLString) {
                        //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                        UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    }
                }
                //: alertController.addAction(action1)
                alertController.addAction(action1)
                //: alertController.addAction(action2)
                alertController.addAction(action2)
                //: present(alertController, animated: true)
                present(alertController, animated: true)
            }
        }
    }
}

//: extension AppWebViewController: WKNavigationDelegate, WKUIDelegate {
extension DeviceViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: clearJSBgColor()
        status()
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: let alertController = UIAlertController(title: nil, message: "Poor network, loading failed", preferredStyle: .alert)
        let alertController = UIAlertController(title: nil, message: (String(kModeVersion.suffix(5)) + "netw" + String(factoryMilitaryPostString.prefix(4)) + " loadi" + String(appTableList)), preferredStyle: .alert)
        //: let action = UIAlertAction(title: "Refresh", style: .default) { _ in
        let action = UIAlertAction(title: (String(kDoingHandState)), style: .default) { _ in
            //: self.reloadWebView()
            self.promptField()
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: present(alertController, animated: true)
        present(alertController, animated: true)
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func reloadWebView() {
    func promptField() {
        //: if self.webView.url != nil {
        if self.webView.url != nil {
            //: self.webView.reload()
            self.webView.reload()
        //: } else {
        } else {
            //: self.beginStartRequest()
            self.color()
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
            //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

    //: func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
    func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
        //: self.reloadWebView()
        self.promptField()
    }

    //: func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
        //: pendingAlertCompletion = completionHandler
        pendingAlertCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(userBiologyLabPath.prefix(5))), message: message, preferredStyle: .alert)
        //: let action = UIAlertAction(title: "OK", style: .default) { _ in
        let action = UIAlertAction(title: "OK", style: .default) { _ in
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = PhoneAdapt.purchase() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback to avoid crash if cannot present
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: pendingConfirmCompletion = completionHandler
        pendingConfirmCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(userBiologyLabPath.prefix(5))), message: message, preferredStyle: .alert)
        //: let okAction = UIAlertAction(title: "OK", style: .default) { _ in
        let okAction = UIAlertAction(title: (String(main_componentPath)), style: .default) { _ in
            //: self.pendingConfirmCompletion?(true)
            self.pendingConfirmCompletion?(true)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(factorySharedProgressUrl)), style: .cancel) { _ in
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(okAction)
        alertController.addAction(okAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = PhoneAdapt.purchase() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback default = false
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: pendingPromptCompletion = completionHandler
        pendingPromptCompletion = completionHandler
        //: let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let doneAction = UIAlertAction(title: "Done", style: .default) { _ in
        let doneAction = UIAlertAction(title: (String(loggerSecondStr.prefix(4))), style: .default) { _ in
            //: let text = alertController.textFields?.first?.text
            let text = alertController.textFields?.first?.text
            //: self.pendingPromptCompletion?(text)
            self.pendingPromptCompletion?(text)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(factorySharedProgressUrl)), style: .cancel) { _ in
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(doneAction)
        alertController.addAction(doneAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = PhoneAdapt.purchase() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback default = nil
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
    }

    //: @available(iOS 15.0, *)
    @available(iOS 15.0, *)
    //: func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
    func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
        //: decisionHandler(.grant)
        decisionHandler(.grant)
    }
}

//: extension AppWebViewController {
extension DeviceViewController {
    /// Ensure any pending JS dialog completion handlers are executed to avoid WKWebView crash
    //: private func finalizePendingJSHandlersIfNeeded() {
    private func sinceSavePolicy() {
        //: if let alertCompletion = pendingAlertCompletion {
        if let alertCompletion = pendingAlertCompletion {
            //: alertCompletion()
            alertCompletion()
            //: pendingAlertCompletion = nil
            pendingAlertCompletion = nil
        }
        //: if let confirmCompletion = pendingConfirmCompletion {
        if let confirmCompletion = pendingConfirmCompletion {
            //: confirmCompletion(false)
            confirmCompletion(false)
            //: pendingConfirmCompletion = nil
            pendingConfirmCompletion = nil
        }
        //: if let promptCompletion = pendingPromptCompletion {
        if let promptCompletion = pendingPromptCompletion {
            //: promptCompletion(nil)
            promptCompletion(nil)
            //: pendingPromptCompletion = nil
            pendingPromptCompletion = nil
        }
    }
    
    /// 通知三方H5刷新金币
    //: func third_jsEvent_refreshCoin() {
    func push() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: dataRawStatus.map{launchTool(ok: $0)}, encoding: .utf8)!) { data, error in
        }
    }
    
    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func deadline() {
        //: self.bridge?.callHandler("onPageShow")
        self.bridge?.callHandler((String(managerLargeTitle.prefix(5)) + "eShow"))
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: dataServiceId.reversed(), encoding: .utf8)!) { data, error in
            //: print("jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
        }
    }
    
    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func system() {
        //: self.bridge?.callHandler("onPageHide")
        self.bridge?.callHandler((String(cacheEndPath.suffix(4)) + "geHide"))
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: userResultDict.map{goHour(total: $0)}, encoding: .utf8)!) { data, error in
            //: print("jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
        }
    }
}
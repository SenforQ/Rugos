
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let viewPowderName:String = "getDestyle change format mic"
fileprivate let const_transformName:String = "viceIDpost no for"

/*: "getFirebaseID" :*/
fileprivate let notiDiskState:String = "groot"
fileprivate let noti_fogMode:[Character] = ["t","F","i","r","e","b","a","s","e","I","D"]

/*: "getAreaISO" :*/
fileprivate let cacheFieldDate:String = "hand"
fileprivate let engineSoundKey:[Character] = ["e","t","A","r","e","a","I","S","O"]

/*: "getProxyStatus" :*/
fileprivate let const_sinceURL:String = "currency house minimum motive layergetPr"
fileprivate let loggerAllowName:String = "azones"

/*: "getMicStatus" :*/
fileprivate let configSearchFlag:[Character] = ["g","e","t","M","i","c","S"]
fileprivate let viewLoadFlag:[Character] = ["t","a","t","u","s"]

/*: "getPhotoStatus" :*/
fileprivate let helperKindValue:String = "getPhotif document"
fileprivate let main_screenList:[Character] = ["o","S","t","a","t","u","s"]

/*: "getCameraStatus" :*/
fileprivate let viewHeadError:[Character] = ["g","e","t","C","a","m"]
fileprivate let controllerBodyUrl:String = "time session tag in saleseraS"

/*: "reportAdjust" :*/
fileprivate let factoryInsideURL:String = "REPOR"
fileprivate let noti_errorFlag:[Character] = ["t"]
fileprivate let controllerTrustKey:String = "secret for filterAdjust"

/*: "requestLocalPush" :*/
fileprivate let panelCommandToken:String = "REQUES"
fileprivate let engineCurfewCount:String = "system sales continue let visibletLoca"

/*: "getLangCode" :*/
fileprivate let parserDittyBagMode:String = "second take house photo layergetL"

/*: "getTimeZone" :*/
fileprivate let user_atWaitCount:[Character] = ["g","e","t","T","i","m","e","Z","o","n"]
fileprivate let formatterFailureDict:[Character] = ["e"]

/*: "getInstalledApps" :*/
fileprivate let networkInstanceFormat:String = "getInmap save space replace day"
fileprivate let notiRationalLaunchMessage:String = "remove indexedApps"

/*: "getSystemUUID" :*/
fileprivate let appFramePath:[Character] = ["g","e","t","S","y","s","t","e","m","U","U"]
fileprivate let notiTabTotalStatus:[Character] = ["I","D"]

/*: "getCountryCode" :*/
fileprivate let poorFirstStr:String = "getCounsub factory true operation"
fileprivate let const_agentToken:String = "take"
fileprivate let sessionValueFormat:String = "ryCodepath valid allow to"

/*: "inAppRating" :*/
fileprivate let viewHourSecret:String = "inAppscene pic back network operation"

/*: "apPay" :*/
fileprivate let helperDomainSecondList:String = "apPayconfirm search normal"

/*: "subscribe" :*/
fileprivate let sessionDigitizerDate:String = "scenterbs"

/*: "openSystemBrowser" :*/
fileprivate let const_objectID:[Character] = ["o","p","e","n","S","y","s","t","e","m","B","r"]
fileprivate let formatterClearHourName:String = "owpowderer"

/*: "closeWebview" :*/
fileprivate let sessionActivityTime:[Character] = ["c","l","o","s","e"]
fileprivate let engineMottleKey:String = "Webviewcolor color server bar"

/*: "openNewWebview" :*/
fileprivate let networkMethodStr:[Character] = ["o","p","e","n","N"]
fileprivate let app_mFlag:[Character] = ["e","w","W","e","b","v","i","e","w"]

/*: "reloadWebview" :*/
fileprivate let sessionScriptError:[Character] = ["r","e","l","o","a","d","W"]
fileprivate let controllerCenterResult:String = "ebsoundiew"

/*: "openSettings" :*/
fileprivate let noti_worldSecret:String = "trusten"

/*: "getNotificationStatus" :*/
fileprivate let parserPerspicuousPath:String = "getNindex create application deadline app"
fileprivate let factoryTitleStatusSecret:[Character] = ["o","t","i","f","i","c","a","t","i","o","n","S","t","a","t","u","s"]

/*: "setScheduledLocalPush" :*/
fileprivate let const_localData:[Character] = ["s","e","t","S","c","h","e","d"]
fileprivate let const_goStr:[Character] = ["u","l","e","d","L","o","c","a"]
fileprivate let modelMainFilterKey:String = "lPushremove carrier"

/*: "typeName" :*/
fileprivate let infoMagnitudeervalName:[UInt8] = [0x8a,0x87,0x8e,0x9b,0xb0,0x9f,0x93,0x9b]

private func kingSize(rational num: UInt8) -> UInt8 {
    return num ^ 254
}

/*: "deviceID" :*/
fileprivate let engineNowBridgeList:[UInt8] = [0x4b,0x4c,0x5d,0x50,0x4a,0x4c,0x30,0x2b]

fileprivate func fromOrigin(state num: UInt8) -> UInt8 {
    let value = Int(num) - 231
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "fireBaseID" :*/
fileprivate let main_fileInFormat:[UInt8] = [0x44,0x49,0x65,0x73,0x61,0x42,0x65,0x72,0x69,0x66]

/*: "areaISO" :*/
fileprivate let serviceStopCurrentSecret:[UInt8] = [0x8d,0x9e,0x91,0x8d,0x75,0x7f,0x7b]

fileprivate func actionOperation(context num: UInt8) -> UInt8 {
    let value = Int(num) - 44
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isProxy" :*/
fileprivate let engineDayMessage:[UInt8] = [0x79,0x78,0x6f,0x72,0x50,0x73,0x69]

/*: "langCode" :*/
fileprivate let app_viewName:[UInt8] = [0x1,0xc,0x3,0xa,0x2e,0x2,0x9,0x8]

private func snapRegion(target num: UInt8) -> UInt8 {
    return num ^ 109
}

/*: "timeZone" :*/
fileprivate let controllerUsKey:[UInt8] = [0x2c,0x31,0x35,0x3d,0x2,0x37,0x36,0x3d]

private func formatCenter(report num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "installedApps" :*/
fileprivate let notiConcealedFlag:[UInt8] = [0x49,0x4e,0x53,0x54,0x41,0x4c,0x4c,0x45,0x44,0x21,0x50,0x50,0x53]

fileprivate func modeDomain(fail num: UInt8) -> UInt8 {
    let value = Int(num) - 224
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "systemUUID" :*/
fileprivate let constFlexibleUrl:[UInt8] = [0x44,0x49,0x55,0x55,0x6d,0x65,0x74,0x73,0x79,0x73]

/*: "countryCode" :*/
fileprivate let data_enterFormat:[UInt8] = [0x24,0x28,0x32,0x29,0x33,0x35,0x3e,0x4,0x28,0x23,0x22]

private func itemYour(never num: UInt8) -> UInt8 {
    return num ^ 71
}

/*: "status" :*/
fileprivate let k_goValue:[UInt8] = [0xfa,0xfb,0xe8,0xfb,0xfc,0xfa]

fileprivate func bodyCapture(report num: UInt8) -> UInt8 {
    let value = Int(num) + 121
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isAuth" :*/
fileprivate let data_nowEventStr:[UInt8] = [0xa5,0xaf,0x7d,0xb1,0xb0,0xa4]

fileprivate func whereGrantObject(raw num: UInt8) -> UInt8 {
    let value = Int(num) - 60
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isFirst" :*/
fileprivate let data_schemeString:[UInt8] = [0x33,0x29,0x1c,0x33,0x28,0x29,0x2e]

private func appearEnableSharedDevice(selected num: UInt8) -> UInt8 {
    return num ^ 90
}

/*: __LocalPush" :*/
fileprivate let main_contactVersion:String = "__Locanow by product loop string"
fileprivate let factoryTunKey:String = "category true toollPush"

/*: "identifier" :*/
fileprivate let loggerUpFormat:[UInt8] = [0xd3,0xce,0xcf,0xd8,0xde,0xd3,0xd0,0xd3,0xcf,0xdc]

fileprivate func receiveRevenue(fire num: UInt8) -> UInt8 {
    let value = Int(num) - 106
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "HTTPProxy" :*/
fileprivate let const_spacePath:String = "HTTPPrdecision ok field manager"
fileprivate let factoryNetworkMode:String = "zoney"

/*: "HTTPSProxy" :*/
fileprivate let user_keyLinkFormat:[Character] = ["H","T","T","P","S","P","r","o","x","y"]

/*: "SOCKSProxy" :*/
fileprivate let controllerContactTime:String = "evaluateCKSP"
fileprivate let app_currentURL:String = "framexy"

/*: "__SCOPED__" :*/
fileprivate let serviceEnterValue:String = "__SCwhere close"
fileprivate let engineCountValue:String = "ad"

/*: "tap" :*/
fileprivate let userHourName:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let routerRawReloadKey:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let k_globalValue:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let engineHourTitle:[Character] = ["p","p","p"]

/*: "Retry After or Go to " :*/
fileprivate let k_packageMode:String = "style contrastRetr"
fileprivate let viewTakeFormat:String = "panel original language point versionr or "
fileprivate let dataConfirmVersion:String = "warn page map prompt birdGo to "

/*: "Feedback" :*/
fileprivate let const_acrossKey:String = "not production available fieldFe"
fileprivate let showHouseToken:[Character] = ["e","d","b","a","c","k"]

/*: " to contact us" :*/
fileprivate let viewToData:[Character] = [" ","t","o"," ","c","o","n","t","a","c","t"," ","u","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let serviceStatusStr     = (String(viewPowderName.prefix(5)) + String(const_transformName.prefix(6)))        // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let notiMicReplaceSecret   = (notiDiskState.replacingOccurrences(of: "root", with: "e") + String(noti_fogMode))      // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let parserSynchroMsg      = (cacheFieldDate.replacingOccurrences(of: "hand", with: "g") + String(engineSoundKey))         // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let configSecureVersion  = (String(const_sinceURL.suffix(5)) + "oxySt" + loggerAllowName.replacingOccurrences(of: "zone", with: "tu"))     // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let main_transformTitle    = (String(configSearchFlag) + String(viewLoadFlag))       // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let kObjectTime  = (String(helperKindValue.prefix(7)) + String(main_screenList))     // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let kResponseUrl = (String(viewHeadError) + String(controllerBodyUrl.suffix(4)) + "tatus")    // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let show_levelCount    = (factoryInsideURL.lowercased() + String(noti_errorFlag) + String(controllerTrustKey.suffix(6)))       // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let main_formOperationError = (panelCommandToken.lowercased() + String(engineCurfewCount.suffix(5)) + "lPush")  // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let formatterBodyEnvironmentFormat      = (String(parserDittyBagMode.suffix(4)) + "angCode")        // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let show_thatPath      = (String(user_atWaitCount) + String(formatterFailureDict))        // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let mainUpwardlyBuildName = (String(networkInstanceFormat.prefix(5)) + "stall" + String(notiRationalLaunchMessage.suffix(6)))   // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let k_areaName    = (String(appFramePath) + String(notiTabTotalStatus))      // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let formatterServerTime   = (String(poorFirstStr.prefix(7)) + const_agentToken.replacingOccurrences(of: "take", with: "t") + String(sessionValueFormat.prefix(6)))     // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let k_systemOfRulesTitle      = (String(viewHourSecret.prefix(5)) + "Rating")        // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let kModeReplaceUpMode            = (String(helperDomainSecondList.prefix(5)))              // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let serviceSecureSpaceName        = (sessionDigitizerDate.replacingOccurrences(of: "center", with: "u") + "cribe")          // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let ratingContextTitle = (String(const_objectID) + formatterClearHourName.replacingOccurrences(of: "powder", with: "s")) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let loggerCognomenAreaUrl     = (String(sessionActivityTime) + String(engineMottleKey.prefix(7)))       // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let formatterToolStatus   = (String(networkMethodStr) + String(app_mFlag))     // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let k_changeOfStateMsg    = (String(sessionScriptError) + controllerCenterResult.replacingOccurrences(of: "sound", with: "v"))      // 重载webView
//: private let openSettings = "openSettings"                   // v2.0.4新增：打开通知设置页
private let user_previousDate = (noti_worldSecret.replacingOccurrences(of: "trust", with: "op") + "Settings")                   // v2.0.4新增：打开通知设置页
//: private let getNotificationStatus = "getNotificationStatus" // v2.0.4新增：获取通知权限
private let cacheTableToken = (String(parserPerspicuousPath.prefix(4)) + String(factoryTitleStatusSecret)) // v2.0.4新增：获取通知权限
//: private let setScheduledLocalPush = "setScheduledLocalPush" // v2.0.5新增：设置定时本地消息推送
private let routerCanId = (String(const_localData) + String(const_goStr) + String(modelMainFilterKey.prefix(5))) // v2.0.5新增：设置定时本地消息推送

//: struct JSMessageModel: HandyJSON {
struct FactoryArgument: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: SaveerCancelControl?
    // 内购/订阅 H5参数
    //: var goodsId: String?     
    var goodsId: String?     // 商品Id
    //: var source: Int?         
    var source: Int?         // 充值来源
    //: var type: Int?           
    var type: Int?           // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?         
    var url: String?         // url
    //: var fullscreen: Int?     
    var fullscreen: Int?     // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?   
    var transparency: Int?   // transparency：0-webview白色背景 1-webview透明背景
    //: var time: [Int]?         
    var time: [Int]?         // 本地推送当天时间（24小时制）
    //: var msg: [String]?       
    var msg: [String]?       // 本地推送文案
}

//: struct UserInfoModel: HandyJSON {
struct SaveerCancelControl: HandyJSON {
    //: var headPic: String?   
    var headPic: String?   // 头像
    //: var nickname: String?  
    var nickname: String?  // 昵称
    //: var uid: String?       
    var uid: String?       // 用户Id
}

//: extension AppWebViewController {
extension DeviceViewController {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func rationalMotiveHouseOfCards(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = FactoryArgument.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case serviceStatusStr:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = PriorityManager.application()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: engineNowBridgeList.map{fromOrigin(state: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case notiMicReplaceSecret:
                //: AppWebViewController.getFireBaseToken { str in
                DeviceViewController.processPolicy { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: main_fileInFormat.reversed(), encoding: .utf8)!: str])
                }
                
            //: case getAreaISO:
            case parserSynchroMsg:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = DeviceViewController.facingPages()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: serviceStopCurrentSecret.map{actionOperation(context: $0)}, encoding: .utf8)!: arr.joined(separator: ",")])
                
            //: case getProxyStatus:
            case configSecureVersion:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = DeviceViewController.present()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: engineDayMessage.reversed(), encoding: .utf8)!: status])
              
            //: case getLangCode:
            case formatterBodyEnvironmentFormat:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_viewName.map{snapRegion(target: $0)}, encoding: .utf8)!: langCode])
                
            //: case getTimeZone:
            case show_thatPath:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: controllerUsKey.map{formatCenter(report: $0)}, encoding: .utf8)!: timeZone])
                
            //: case getInstalledApps:
            case mainUpwardlyBuildName:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: notiConcealedFlag.map{modeDomain(fail: $0)}, encoding: .utf8)!: apps])
                
            //: case getSystemUUID:
            case k_areaName:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: constFlexibleUrl.reversed(), encoding: .utf8)!: uuid])
                
            //: case getCountryCode:
            case formatterServerTime:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_enterFormat.map{itemYour(never: $0)}, encoding: .utf8)!: countryCode])
                
            //: case inAppRating:
            case k_systemOfRulesTitle:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                DeviceViewController.track()

            //: case apPay:
            case kModeReplaceUpMode:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.item(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: k_goValue.map{bodyCapture(report: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case serviceSecureSpaceName:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.item(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: k_goValue.map{bodyCapture(report: $0)}, encoding: .utf8)!: success])
                    }
                }
                
            //: case openSystemBrowser:
            case ratingContextTitle:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }
                
            //: case closeWebview:
            case loggerCognomenAreaUrl:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.emptyBegin()
                
            //: case openNewWebview:
            case formatterToolStatus:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                    //: let transparency = model.transparency,
                    let transparency = model.transparency,
                    //: let fullscreen = model.fullscreen {
                    let fullscreen = model.fullscreen {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    DeviceViewController.administer(urlStr, transparency, fullscreen)
                }
                
            //: case reloadWebview:
            case k_changeOfStateMsg:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.promptField()
            
            //: case openSettings:
            case user_previousDate:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])
                //: if let url = URL(string: UIApplication.openSettingsURLString) {
                if let url = URL(string: UIApplication.openSettingsURLString) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                }
                
            //: case setScheduledLocalPush:
            case routerCanId:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])
                //: LocalPushScheduler.shared.schedule(times: model.time ?? [], contents: model.msg ?? [])
                BindingCharacter.shared.start(times: model.time ?? [], contents: model.msg ?? [])
                
            //: case getNotificationStatus:
            case cacheTableToken:
                //: AppPermissionTool.shared.requestNotificationPermission { auth, isFirst in
                SalesBuilder.shared.frame { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_nowEventStr.map{whereGrantObject(raw: $0)}, encoding: .utf8)!: auth, String(bytes: data_schemeString.map{appearEnableSharedDevice(selected: $0)}, encoding: .utf8)!: isFirst])
                }
            
            //: case getMicStatus:
            case main_transformTitle:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                SalesBuilder.shared.that { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_nowEventStr.map{whereGrantObject(raw: $0)}, encoding: .utf8)!: auth, String(bytes: data_schemeString.map{appearEnableSharedDevice(selected: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case getPhotoStatus:
            case kObjectTime:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                SalesBuilder.shared.revenue { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_nowEventStr.map{whereGrantObject(raw: $0)}, encoding: .utf8)!: auth, String(bytes: data_schemeString.map{appearEnableSharedDevice(selected: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case getCameraStatus:
            case kResponseUrl:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                SalesBuilder.shared.doCan { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName, String(bytes: data_nowEventStr.map{whereGrantObject(raw: $0)}, encoding: .utf8)!: auth, String(bytes: data_schemeString.map{appearEnableSharedDevice(selected: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case reportAdjust:
            case show_levelCount:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        PriorityManager.localGroup(token: token, count: count)
                    //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        PriorityManager.device(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case main_formOperationError:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: infoMagnitudeervalName.map{kingSize(rational: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                DeviceViewController.pastComponentManager(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event
//: extension AppWebViewController {
extension DeviceViewController {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func administer(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = DeviceViewController()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        PhoneAdapt.purchase()?.present(vc, animated: true)
    }
    
    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func pastComponentManager(_ model: FactoryArgument) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()
                
            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(cacheWarnVersionURL)" + (String(main_contactVersion.prefix(6)) + String(factoryTunKey.suffix(5)))
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: loggerUpFormat.map{receiveRevenue(fire: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }
                
            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }
    
    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func processPolicy(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
            //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func facingPages() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func present() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if DeviceViewController.noCenter() || DeviceViewController.contrastLocal() {
            //: return true
            return true
        }
        //: return false
        return false
    }
    
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func noCenter() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(String(const_spacePath.prefix(6)) + factoryNetworkMode.replacingOccurrences(of: "zone", with: "ox"))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(String(user_keyLinkFormat))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(controllerContactTime.replacingOccurrences(of: "evaluate", with: "SO") + app_currentURL.replacingOccurrences(of: "frame", with: "ro"))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }
    
    /// 判断用户设备是否开启代理VPN
   /// - Returns: false: 未开启  true: 开启
   //: class func isUsedVPN() -> Bool {
   class func contrastLocal() -> Bool {
       //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
       guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
       //: guard let dict = proxy as? [String: Any] else { return false }
       guard let dict = proxy as? [String: Any] else { return false }
       //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
       guard let scopedDic = dict[(String(serviceEnterValue.prefix(4)) + "OPED_" + engineCountValue.replacingOccurrences(of: "ad", with: "_"))] as? [String: Any] else { return false }
       //: for keyStr in scopedDic.keys {
       for keyStr in scopedDic.keys {
           //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
           if keyStr.contains((String(userHourName))) || keyStr.contains((String(routerRawReloadKey))) || keyStr.contains((String(k_globalValue))) || keyStr.contains((String(engineHourTitle))){
               //: return true
               return true
           }
       }
       //: return false
       return false
   }
    
    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func track() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
        //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }
    
    // MARK: - Event
    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func item(productId: String, source: Int = -1, payType: PrivacyVersionBar, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        SuccessData.page()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        OfCharacter.shared.handle(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            SuccessData.all()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    SuccessData.object(self.SuccessDataStr())
                    
                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.push()
                    
                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
    
    func SuccessDataStr() -> String{
        return (String(k_packageMode.suffix(4)) + "y Afte" + String(viewTakeFormat.suffix(5)) + String(dataConfirmVersion.suffix(6))) + "\"" + (String(const_acrossKey.suffix(2)) + String(showHouseToken)) + "\"" + (String(viewToData))
    }
}
